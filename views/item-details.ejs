<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= product.title %> - Details</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    body {
      background: #f5f7fa;
      color: #333;
    }
    .container {
      max-width: 900px;
      margin: 40px auto;
      background: white;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 6px 18px rgba(0, 0, 0, 0.08);
    }

    .item-image {
      width: 100%;
      max-height: 400px;
      object-fit: contain;
      border-radius: 12px;
    }

    .item-title {
      font-size: 28px;
      margin-top: 20px;
      color: #111827;
    }

    .item-description {
      font-size: 16px;
      margin: 15px 0;
      color: #555;
      line-height: 1.6;
    }

    .item-meta {
      font-size: 16px;
      margin: 8px 0;
    }

    .item-meta strong {
      color: #374151;
    }

    .buttons {
      margin-top: 25px;
      display: flex;
      gap: 15px;
    }

    .buttons button {
      padding: 10px 22px;
      font-size: 16px;
      border: none;
      border-radius: 6px;
      cursor: pointer;
      transition: background-color 0.2s ease;
      font-weight: 500;
    }

    .order-btn {
      background: #10b981;
      color: white;
    }

    .order-btn:hover {
      background: #059669;
    }

    .chat-btn {
      background: #3b82f6;
      color: white;
    }

    .chat-btn:hover {
      background: #2563eb;
    }

    .back-btn {
      display: block;
      text-align: center;
      color: white;
      float: right;
      text-decoration: none;
      font-size: 15px;
    }

    .back-btn:hover {
      text-decoration: underline;
    }
    
    .back-btn {
      display: block;
      text-align: center;
      margin-top: 20px;
      color: #007bff;
      text-decoration: none;
      font-size: 15px;
    }

    .back-btn:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <%- include('partials/header', { username: username, title: 'Dashboard' })
    %>
  <main>
  <div class="container">
    <img class="item-image" src="<%= product.imageUrl %>" alt="<%= product.title %>">
    <h1 class="item-title"><%= product.title %></h1>
    <p class="item-description"><%= product.description %></p>
    <p class="item-meta"><strong>Price:</strong> ₹<%= product.price.toLocaleString() %></p>
    <p class="item-meta"><strong>Category:</strong> <%= product.category %></p>
    <p class="item-meta"><strong>Posted by:</strong> <%= product.sellerId.name %></p>


    <div class="buttons">
      <a href="/checkout/<%= product._id %>">
        <button type="button" class="order-btn">Order Now</button>
      </a>
    </div>
    <a class="back-btn" href="/dashboard">Back to Dashboard</a>
  </div>
  
  </main>
  <%- include('partials/footer') %>
</body>
</html>
